# 待办应用产品需求文档

> 版本：v1.0（已确认）
> 日期：2026-09-13
> 定位：清新治愈系待办应用，本地优先，未来支持多设备同步

---

## 一、产品定位

**一句话**：一个让人放松、愿意主动打开的待办应用。

**核心差异点**：
- 视觉上：清新、治愈、不压迫
- 交互上：简单、不打断、不制造焦虑

**目标用户**：学生、上班族、自由职业者，尤其是对"压力感待办工具"反感的人。

**平台形态**：响应式网页 / PWA，一套代码适配手机与电脑。

---

## 二、范围（已确认）

### P0：第一版 MVP（本地版，本次开发范围）

| 模块 | 能力 | 验收标准 |
|---|---|---|
| 任务管理 | 新增任务 | 输入文字回车即添加，立刻出现在列表 |
| | 勾选完成 | 点击切换完成状态，完成后样式变淡/划线 |
| | 编辑任务 | 可修改任务文字 |
| | 删除任务 | 从列表移除 |
| 任务列表 | 列表展示 | 未完成在上、已完成在下 |
| 数据 | 本地持久化 | 存 localStorage，刷新不丢失 |
| 视觉 | 治愈系风格 | 单套主题，符合第三节视觉规范 |
| 适配 | 响应式 | 手机宽度下可正常使用 |

### P1：本地增强（本期不做，后续迭代）

标签、优先级、截止日期、搜索、筛选、排序、回收站、番茄钟、分页、空状态、Toast 反馈。

> 说明：这些功能会显著增加数据模型与数据流复杂度。等有真实使用习惯后再加，才知道该怎么做。

### P2：多端与主题（本期不做）

账号系统、云同步、主题切换、自定义背景、支付、二次元角色提醒、数据统计。

---

## 三、视觉规范（清新治愈风）

**关键词**：晨雾、暖金、低饱和、柔和、留白、通透。

- **背景**：暖金 → 暖绿的柔和渐变，带轻微光晕
- **卡片**：半透明毛玻璃，圆角柔和，阴影轻柔
- **文字**：深灰 / 暖褐，不要纯黑
- **按钮**：暖金色，悬停柔和过渡
- **整体**：不刺眼、不压迫、看着舒心

---

## 四、数据模型（为未来同步预留）

即使本期不实现同步，数据结构从一开始就按"未来要同步"设计，避免日后重构。

```ts
interface Task {
  id: string;          // 唯一 ID（crypto.randomUUID）
  title: string;       // 任务内容
  completed: boolean;  // 是否完成
  createdAt: number;   // 创建时间戳
  updatedAt: number;   // 最后修改时间戳（同步用）
  deletedAt: number | null; // 软删除标记，预留（当前 UI 直接隐藏）
}
```

**约定**：
- 所有时间用时间戳（number），不用字符串
- 删除采用软删除，便于未来同步与回收站
- 存储 key 固定，如 `todo-app:tasks`

**未来同步策略（待定，不影响本期）**：默认"最后修改时间优先"（Last-Write-Wins）。

---

## 五、技术栈（已确认）

| 层 | 技术 |
|---|---|
| 构建 | Vite |
| 框架 | React + TypeScript |
| 样式 | Tailwind CSS |
| 状态 | React 内置状态 + 自定义 hook（暂不引入状态库） |
| 存储 | localStorage |
| 部署 | Vercel（静态托管） |

---

## 六、开发顺序

1. 初始化 Vite + React + TS + Tailwind 项目
2. 搭静态界面，先定下治愈系视觉
3. 实现任务新增 / 完成 / 编辑 / 删除
4. 接入 localStorage 持久化
5. 响应式适配
6. 部署到 Vercel

**不跳步**：每一步跑通再走下一步。

---

## 七、一句话总结

**极简本地 MVP：新增 / 完成 / 编辑 / 删除 / 列表 / 本地存储 + 治愈系视觉，数据模型按未来同步预留，部署走 Vercel。**
