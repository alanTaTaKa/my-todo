# 待办应用 · 项目总结与交接

> 日期：2026-09-19
> 当前版本：v5.2.0（版本数据见 `src/lib/version.ts`）
> 用途：开启新对话时用本文档快速恢复上下文

---

## 一、项目定位

一个**清新治愈系**待办应用，本地优先、可离线使用，未来支持多设备云同步。

- 平台形态：响应式网页 / PWA（一套代码适配手机与电脑）
- 部署：前端已手动部署到 Vercel（用户自行维护，push 即更新）
- 设计基调：暖金、暖绿、低饱和、毛玻璃、留白

原始需求文档：`从0开始需求.md`

---

## 二、技术栈与命令

| 层 | 技术 |
|---|---|
| 构建 | Vite 8 |
| 框架 | React 19 + TypeScript 6 |
| 样式 | Tailwind CSS v4（`@tailwindcss/vite` 插件） |
| 状态 | React 内置状态 + 自定义 hook（无状态库） |
| 存储 | localStorage（本地数据）+ httpOnly Cookie（登录会话） |
| 后端 | Node + Hono + TypeScript + Drizzle + PostgreSQL（`server/`） |
| PWA | `vite-plugin-pwa`（Workbox 预缓存 + Web App Manifest） |
| Lint / 测试 | oxlint / `node:test`（经 `tsx --test` 运行） |

```bash
# 前端
npm install      # 安装依赖
npm run dev      # 本地开发（默认 5173）
npm run build    # tsc -b && vite build
npm run lint     # oxlint
npm test         # 纯逻辑单测（tsx --test，node:test）

# 后端（账号系统，可选登录）
cd server
npm install
cp .env.example .env     # 填写 DATABASE_URL（Neon/Supabase/本地均可）
npm run db:migrate       # 应用迁移；或 npm run db:push 直接同步
npm run dev              # 启动 API（默认 8787）
```

> 纯逻辑已有单测（`src/lib/*.test.ts`，覆盖合并/LWW、日期筛选、颜色对比度、排序、主题净化）；其余验证方式：`build` + `lint` + 启动 dev server 确认 200。

---

## 三、目录结构

```
src/
  App.tsx                    主界面与所有状态串联
  Root.tsx                   路由分发：`/` 宣传页、`/app` 应用
  main.tsx                   入口
  index.css                  主题/全局样式（@theme + :root[data-theme]）
  landing/
    LandingPage.tsx          宣传落地页（Hero/价值/功能/升级/FAQ/页脚）
    AppPreview.tsx           代码仿制的应用界面 + mini 试用
    ThemeShowcase.tsx        主题橱窗（点选实时换肤）
  types.ts                   全局类型（Task/Tag/UserProfile/筛选/排序/优先级）
  vite-env.d.ts              Vite 与 PWA 客户端类型引用
  components/
    AppHeader.tsx            首页标题/副标题（点击可编辑）
    Modal.tsx                弹窗公共外壳（遮罩/标题栏/关闭/Esc）
    TaskInput.tsx            新增任务输入框
    TaskItem.tsx             任务项（完成/编辑/标签/优先级/截止/删除）
    FilterBar.tsx            搜索 + 状态 + 日期 + 标签 + 排序 chips
    Pagination.tsx           分页（5/10/20/50）
    DueBadge.tsx             截止日期徽章（逾期高亮）
    PriorityBadge.tsx        优先级徽章
    TagChip.tsx              标签色块
    TagManager.tsx           标签新建/改名/删除
    RecycleBin.tsx           回收站弹窗（恢复/永久删除/清空）
    StatsPanel.tsx           数据统计弹窗（按标签维度）
    VersionPanel.tsx         版本更新弹窗
    ThemePanel.tsx           主题面板（模式/预设/自定义/自选配色/我的配色）
    SavedPalettes.tsx        「我的配色」列表（选中/改名/删除）
    AuthPanel.tsx            账号弹窗（登录/注册/登出）
    FeedbackPanel.tsx        意见反馈弹窗
    UpgradePrompt.tsx        升级提示 + 兑换码输入（供升级弹窗复用）
    UpgradePanel.tsx         升级版弹窗（权益说明 + 兑换）
  hooks/
    useTasks.ts              任务状态与增删改（本地存储）
    useTags.ts               标签状态
    useTheme.ts              主题状态、注入自定义主题 CSS、持久化
    useAuth.ts               账号状态（读取 / 登录 / 注册 / 登出）
    useEntitlements.ts       升级权益状态（拉取 / 本地缓存 / 兑换）
    useProfile.ts            标题/副标题（本地 + 同步）
    useSync.ts               云同步引擎（推送 / 拉取 / 合并 / 状态）
    useDraggable.ts          弹窗拖动（Pointer Events）
  lib/
    storage.ts               localStorage 读写与数据校验（统一入口）
    api.ts                   统一 fetch 封装（credentials/JSON/错误文案）
    auth.ts                  鉴权 API 客户端（fetch，带 Cookie）
    entitlements.ts          权益 API 客户端 + Mode→功能映射
    sync.ts                  同步 API 客户端（pull / push + SyncStatus）
    merge.ts                 按 updatedAt 的逐条 LWW 合并
    feedback.ts              意见反馈 API 客户端
    date.ts                  日期时间：格式化、逾期判断、日期筛选
    priority.ts              优先级色板
    tags.ts                  标签色板
    sort.ts                  排序逻辑
    themes.ts                视觉模式（Mode）+ 预设主题定义与解析
    palettes.ts              推荐配色（双色/三色）
    color.ts                 颜色数学（hex/hsl/对比度/混色）
    customTheme.ts           任意颜色 → 完整主题变量推导
    version.ts               版本与更新日志数据
    *.test.ts                纯逻辑单测（merge/color/date/sort/customTheme）
index.html                   首屏防闪烁内联脚本
vite.config.ts               React + Tailwind + PWA 插件 + /api 开发代理
docker-compose.dev.yml       本地 PostgreSQL（开发用）
vercel.json                  生产环境 /api → Render 重写（同源代理）
```

```
public/
  favicon.svg                站点图标（暖金对勾）
  pwa-192x192.png            应用图标 192
  pwa-512x512.png            应用图标 512
  maskable-512x512.png       自适应图标（Android 蒙版）
  apple-touch-icon.png       iOS 主屏图标 180
```

```
server/                      后端（账号 + 同步 + 反馈，独立 npm 包）
  package.json               Hono / Drizzle / pg / tsx / drizzle-kit
  tsconfig.json              Node ESM 严格配置
  drizzle.config.ts          Drizzle Kit 配置
  drizzle/                   生成的 SQL 迁移（0000 账号 / 0001 任务标签 / 0002 永久删除 / 0003 配色 / 0004 设置与反馈 / 0005 权益）
  .env.example               DATABASE_URL / DATABASE_URL_DIRECT / PORT / APP_ORIGIN 等
  src/
    index.ts                 应用入口（CORS / 日志 / 健康检查 / 路由挂载）
    env.ts                   环境变量读取与校验
    db/
      schema.ts              users / sessions / tasks / tags / palettes / profiles / feedback
      index.ts               Drizzle + pg 连接池
    auth/
      routes.ts              register / login / logout / me / 注销账号（DELETE /account）
      middleware.ts          会话 Cookie 校验（requireAuth / optionalAuth）
      password.ts            scrypt 哈希与校验
      session.ts             会话令牌生成与哈希
    sync/
      routes.ts              GET /api/sync?since= + POST /api/sync（LWW upsert）
    entitlements/
      routes.ts              GET /api/me/entitlements + POST /api/me/redeem（权益 / 兑换码）
    feedback/
      routes.ts              POST /api/feedback（免登录，记录到 feedback 表）
    middleware/
      rate-limit.ts          简单内存限流（auth / feedback / sync）
```

---

## 四、数据模型（`src/types.ts`）

```ts
interface Task {
  id: string
  title: string
  completed: boolean
  priority: 'none' | 'low' | 'medium' | 'high'
  dueDate: number | null      // 时间戳，精确到分钟
  createdAt: number
  completedAt: number | null  // 完成时间（不展示）
  updatedAt: number
  deletedAt: number | null    // 软删除（回收站）
  purgedAt: number | null     // 永久删除（同步墓碑，不再展示）
  tagIds: string[]
}

interface Tag {
  id: string
  name: string
  color: 'gold' | 'sage' | 'sky' | 'rose' | 'lavender' | 'clay'
  createdAt: number
  updatedAt: number
  deletedAt: number | null    // 软删除（同步墓碑）
}

interface UserProfile {
  title: string                // 首页主标题
  subtitle: string             // 副标题
  updatedAt: number
}
```

**localStorage 键**

| 键 | 内容 |
|---|---|
| `todo-app:tasks` | 任务数组 |
| `todo-app:tags` | 标签数组 |
| `todo-app:theme` | 主题 id（预设 / `custom` / `system`） |
| `todo-app:entitlements` | 升级权益缓存（离线宽限，登录后以服务端为准） |
| `todo-app:custom-theme` | 当前生效的自定义配色 |
| `todo-app:custom-theme-css` | 生成好的自定义主题 CSS（防闪烁缓存） |
| `todo-app:custom-palettes` | 「我的配色」保存列表 |
| `todo-app:sync-user` | 上次同步的账号 id（用于换号时隔离本地数据） |
| `todo-app:profile` | 首页主标题 / 副标题 |

> 所有读写都经 `src/lib/storage.ts`，带 try/catch 与数据校验（防脏数据/隐私模式）。

---

## 五、已实现功能

### P0 核心（本地版）
- 任务新增、编辑、勾选完成、删除
- 未完成在上、已完成分组；空状态提示
- localStorage 持久化

### P1 体验
- **标签**：6 色新建、改名、删除；给任务打标签；按标签筛选
- **搜索**：标题实时模糊匹配
- **筛选**：状态（全部/未完成/已完成）+ 日期（全部/今天/明天/近7天/已逾期/无日期）+ 标签，可叠加
- **排序**：创建时间 / 优先级 / 截止日期，支持方向切换；无日期置后
- **分页**：5/10/20/50，筛选变化自动回第 1 页
- **优先级**：无/低/中/高，彩色徽章
- **截止日期**：精确到分钟，逾期标红
- **回收站**：软删除、恢复、永久删除、清空
- **数据统计**：按标签维度看完成率、任务总数、已完成、今日到期、逾期
- **版本信息**：底部栏入口，可查看更新日志（数据本地维护，新增版本在 `VERSIONS` 顶部追加）
- **一键删除所有任务**：移到回收站、可恢复，并跨设备同步（底部栏「全部删除」，有确认）
- **标题可编辑**：首页主标题 / 副标题点击即可修改，跨设备同步
- **意见反馈**：底部栏「反馈」入口，提交到后端 `feedback` 表，支持匿名与联系方式

> 番茄钟经用户确认**不做**。

### P3 账号与云同步
- **账号（Phase 1）**：邮箱 + 密码注册/登录/登出；DB 会话 + httpOnly Cookie；密码显示切换；底部栏「账号」入口
- **云同步（Phase 2）**：登录后任务与标签自动同步；冲突逐条 LWW（`updatedAt`）；删除用软删除墓碑；服务端 `syncedAt` 增量游标
- **自定义主题同步（Phase 4）**：「我的配色」（名称 + 颜色配置）跨端同步；**当前选中的主题保持本机**不同步
- **同步增强（Phase 3）**：
  - 永久删除 / 清空回收站用 `purgedAt` 墓碑跨端同步，各端回收站一起清空
  - 换账号登录自动清空本地任务/标签、以云端为准（`todo-app:sync-user` 记录）
  - 增量推送（按本地内容快照判断变更，分批 500 条），失败指数退避（5s→15s→45s，上限 5min）
  - 离线检测：离线显示「等待联网」，联网/聚焦/页面可见时自动补同步
  - 轮询：页面可见每 5s、后台每 60s；同步进行中的触发会补跑而非丢失
- 账号弹窗显示同步状态（同步中 / 刚刚同步 / 离线 / 失败）并提供「立即同步」
- **不登录仍为纯本地模式**，老功能不受影响

### PWA（可安装 / 离线）
- `vite-plugin-pwa`（Workbox `generateSW`）预缓存应用外壳（HTML/JS/CSS/图标）
- 可安装到桌面/主屏，`display: standalone` 独立窗口
- 离线可打开使用（数据在 localStorage），同步失败自动降级为「离线，等待联网」
- Manifest：`manifest.webmanifest`（名称、图标 192/512 + maskable、theme/background）
- `/api/*` 不做离线回退（`navigateFallbackDenylist`），始终走网络
- 状态栏 `theme-color` 跟随主题（`useTheme` 里读取 `--bg-base` 动态更新）
- 更新策略：`registerType: 'autoUpdate'`，`main.tsx` 中 `registerSW({ immediate: true })`

### 宣传落地页（拉新）
- 路由：`/` 落地页、`/app` 应用；`src/Root.tsx` 轻量分发（不引路由库）
- Hero 主 CTA「免注册，立即开始」→ `/app`；含**代码仿制应用预览 + mini 试用**（仅本页状态，不写本地数据）
- 主题橱窗：26 套 + 3 分组，点选即时换肤（改 `document.documentElement.dataset.theme`，不影响应用持久化）
- 价值点 / 功能 / 升级版 / FAQ / 页脚（隐私政策、服务条款为占位）
- PWA `start_url` 改为 `/app`；`vercel.json` 增加 SPA fallback rewrite，保证 `/app` 直达

### P2 主题系统（本项目重点）
- 主列表为极简 12 套 + 跟随系统；二次元 / 国风主题并入「自定义主题」内展示
- **升级版（买断）**：仅「自定义主题」入口加锁（内含二次元 / 国风风格推荐）；底部「升级版」入口 + 升级弹窗，凭账号权益（兑换码）解锁；标题 / 副标题仅升级后可改（基础版纯文本、点击不弹窗、无锁标）
- 预设主题 **26 套 + 跟随系统**：
  - 极简 12 套：清新、暗色、松绿、碧山、天水碧、小雾、退红、桃夭、秋波蓝、浅云、明月珰、紫罗兰
  - 国风 2 套：青黛、胭脂
  - 二次元 12 套：486、银发精灵、幼女、红鬼、青鬼、强欲、太阳公主、松墨、狐狸、6154、加护、神
- **自定义主题**：19 组推荐配色，分「双色搭配」「三色搭配」一键应用
- **自选配色**：取色器 + 十六进制输入，实时预览，自动推导完整主题并保证对比度
- **我的配色**：保存自选方案，自动命名（「我的配色 N」），支持改名、删除
- 主题弹窗**可拖动**（拖动标题栏）
- 主题选择持久化、刷新不丢失、首屏无闪烁

---

## 六、主题系统原理（重点）

### 变量层
- `src/index.css` 中 `@theme` 定义基础色与语义 token：
  - 颜色：`--color-cream/sand/gold/gold-soft/sage/sage-soft/ink/ink-soft`
  - 语义：`--color-surface/surface-soft/surface-2/surface-strong/surface-solid/line/line-soft/overlay/on-accent`
  - chip：`--color-chip/chip-line/chip-text`
  - `--color-sage-text`：用于「已完成」文字（`color-mix` 自适应明暗）
- 每个主题在 `:root[data-theme="<id>"]` 覆盖上述变量；`body` 背景为 `--bg-base` 纯色（`--bg-glow-*` / `--bg-gradient` 仍保留定义，暂未使用）

### 逻辑层
- `src/lib/themes.ts`：`ThemeId` / `ModeId`、`THEMES`、`MODES`、`themesForMode` / `themesInMode` / `themeMode`、`THEME_IDS`（含 `custom`，用于校验）、`resolveTheme`
- `src/hooks/useTheme.ts`：
  - `themeId` 持久化到 `todo-app:theme`
  - 给 `<html>` 设置 `data-theme`
  - `custom` 时用 `deriveCustomTheme` 生成 CSS 注入 `<style id="custom-theme-style">`，并缓存到 localStorage
  - 跟随系统时监听 `prefers-color-scheme`
- `src/lib/customTheme.ts`：任意 2~3 色 → 完整主题变量（自动选择深浅文字方向、推导 hover、chip 底色、边框、遮罩、背景渐变），**内置对比度迭代**
- `src/lib/color.ts`：hex/hsl 转换、亮度、对比度、混色、`normalizeHex`
- `src/lib/palettes.ts`：`DUAL_PALETTES` / `TRI_PALETTES`
  - 双色：`[主色/背景, 辅助色]`
  - 三色：`[背景, 辅助, 强调]`
- `index.html` 内联脚本：首屏读取主题 id 与缓存 CSS，先设 `data-theme` 再渲染，避免闪烁

### 语义约定
- **强调色**（gold）＝主动作：添加按钮、未完成勾选框、选中 chip、焦点
- **辅助色**（sage）＝完成态：完成后勾选框描边/淡底、已完成分区标题圆点、统计「已完成」数字、空状态
- 暗色模式下文字对比度已按 WCAG 校验（自定义配色全部 0 FAIL，最低约 4.6:1）

---

## 七、视觉规范（清新治愈风）

关键词：晨雾、暖金、低饱和、柔和、留白、通透。

- 背景：纯色（`--bg-base`，已移除角落渐变光晕）
- 卡片：半透明毛玻璃 + 柔和圆角 + 轻阴影
- 文字：深灰/暖褐，不用纯黑
- 按钮：暖金色，柔和过渡
- 弹窗遮罩：纯半透明黑（浅色 0.4 / 暗色 0.6），**不使用模糊**

---

## 八、开发约定与注意事项

- **TypeScript 严格**：`noUnusedLocals`、`noUnusedParameters`、`verbatimModuleSyntax`（类型导入必须 `import type`）、`erasableSyntaxOnly`
- **不要加注释**（除非确有必要，如空 catch）
- 样式统一走语义 token，禁止硬编码白色半透明（否则暗色主题会失效）
- 修改后必须跑 `npm run build`、`npm run lint` 和 `npm test`
- 版本号维护：在 `src/lib/version.ts` 的 `VERSIONS` **顶部**追加新条目；`CURRENT_VERSION` 自动取首项。按阶段合并为 5 个大版本（1 本地核心 / 2 主题 / 3 账号 / 4 云同步 / 5 PWA 与个性化），每版只列主要内容
- 颜色相关改动建议做对比度校验（可写临时脚本用真实推导函数验证）
- `README.md` 目前仍是 Vite 默认内容，未整理

---

## 九、账号 + 云同步（Phase 1 & 2 已完成）

### 已确认决策
| 项 | 选择 |
|---|---|
| 后端 | **Node + Hono + TypeScript** |
| 数据库 | **PostgreSQL**（Neon/Supabase 托管）+ Drizzle |
| 鉴权 | 邮箱 + 密码 + **DB 会话 + httpOnly Cookie** |
| 密码哈希 | `node:crypto` 的 scrypt（零原生依赖） |
| 同步冲突 | **逐条 LWW**（按 `updatedAt`）+ 软删除 |
| 部署 | 前端 Vercel；后端 Fly.io / Render |
| 密码找回 | **暂不做**（注册页提示牢记密码；找回留到后期，可选用一次性恢复码或邮件重置） |

### 关键 UX 决策
**登录可选**：不登录 = 现在的纯本地模式；登录后才启用同步。老功能不受影响。

### 仓库结构
同一仓库新增 `server/` 目录（前端主逻辑不动）；开发时 Vite 把 `/api` 代理到本地后端。

### Phase 1 交付物（账号系统，不做同步）— ✅ 已完成
- `server/` 脚手架：Hono + TS + Drizzle + 环境变量（`@hono/node-server` / `pg` / `drizzle-kit` / `tsx`）
- 数据表：`users`（email 唯一、password_hash、created_at）、`sessions`（token_hash 唯一、user_id、expires_at）
  - 迁移：`server/drizzle/0000_init.sql`（`npm run db:generate` 生成）
- 接口：`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`（另有 `GET /api/health`）
- 安全：scrypt（N=16384, r=8, p=1）哈希；会话令牌仅存 sha256 哈希；`httpOnly` Cookie（开发 `SameSite=Lax`、生产默认 `SameSite=None; Secure`，可用 `COOKIE_SAME_SITE` 覆盖）；登录错误统一文案
- 前端：`src/lib/auth.ts` API 客户端、`useAuth`、`AuthPanel` 弹窗、底部栏「账号」入口、登出
- 开发代理：`vite.config.ts` 将 `/api` 代理到 `http://localhost:8787`；CORS 由 `APP_ORIGIN` 控制

**本地跑起来**：在 `server/` 按 `.env.example` 建 `.env`（需一个 PostgreSQL），`npm run db:migrate`（或 `db:push`）后 `npm run dev`；前端 `npm run dev` 即可。

### Phase 2（同步引擎，任务 + 标签）— ✅ 已完成
- 数据模型：`Tag` 已补 `updatedAt` / `deletedAt`；标签删除改软删除
- 服务端表：`tasks` / `tags`（均含 `user_id`、`updated_at`、`deleted_at`、`synced_at`，复合主键 `(user_id, id)`）；迁移 `server/drizzle/0001_tasks_tags_sync.sql`
- 接口：`GET /api/sync?since=`（按 `synced_at` 增量，含 2s 重叠防漏）、`POST /api/sync`（事务内按 `updated_at` LWW upsert，返回落库权威值供纠正）
- 客户端：`useSync` 登录后启用；本地优先；LWW 合并 `lib/merge.ts`；`cursor` + `latest` 引用；拉取用服务端 `serverTime` 游标
- 触发：登录全量合并（幂等）+ 改动后 1.2s 防抖 + 窗口聚焦 + `online` + 可见时每 5s / 后台每 60s 轮询
- 验收思路：两台设备登录同账号，任务/标签增删改最终一致

### Phase 3（同步增强）— ✅ 已完成
- 永久删除跨端：`Task.purgedAt` + 服务端 `purged_at` 列（迁移 `0002_task_purged_at.sql`）；`purgeTask` / `emptyTrash` 写墓碑，合并时忽略未知墓碑避免回收站复活
- 删除终态：服务端对 `tasks.purged_at`、`tags.deleted_at`、`palettes.deleted_at` 加写保护——已永久删除/已删标签配色不可被陈旧设备以更新的时间戳复活；任务**软删除仍可 LWW 恢复**
- 换账号隔离：`todo-app:sync-user` 记录上次同步账号，换号时 `resetTasks` / `resetTags` 清空本地并以云端为准（首次同步仅拉取不推送）
- 增量推送：按「本地内容快照」判断哪些记录需要推送（不依赖时钟），每批 500 条分批上传，避免超大任务量触发 413
- 失败指数退避：5s → 15s → 45s … 上限 5min；成功后重置
- 离线检测：`navigator.onLine` + `online` 事件，状态显示「离线，等待联网」

### Phase 4（用户设置同步）— 部分完成
- ✅ 自定义主题（「我的配色」名称 + 颜色）同步：客户端 `SavedPalette` 加 `createdAt/updatedAt/deletedAt`，服务端新增 `palettes` 表（迁移 `0003_palettes.sql`），随 `/api/sync` 一起 LWW 同步
- ✅ 首页主标题 / 副标题同步：客户端 `UserProfile`（`useProfile`），服务端 `profiles` 表（迁移 `0004`），随 `/api/sync` LWW 同步
- ✅ 当前选中的主题（`todo-app:theme` / `todo-app:custom-theme`）**不同步**，保持设备本地
- ✅ 意见反馈：`POST /api/feedback`（`optionalAuth`，免登录可匿名）+ `feedback` 表（迁移 `0004`）
- ⬜ 背景等其它用户设置可按需扩展（同一套模式）
- ⬜ 多端联调压测与正式部署收尾

### 部署（全托管：Vercel + Render + Neon）
前端 Vercel、后端 Render、数据库 Neon，浏览器只访问 Vercel 域名，`/api` 由 Vercel `vercel.json` 反代到 Render（同源，免 CORS，Cookie 用 `SameSite=Lax`）。

- **本地**：`docker compose -f docker-compose.dev.yml up -d` 起 PostgreSQL → `server/.env` 填 `DATABASE_URL` → `npm run db:migrate`。
- **Neon**：拿 pooled（`DATABASE_URL`，应用用）与 direct（`DATABASE_URL_DIRECT`，迁移用）两条串，均需 `sslmode=require`。
- **Render**：Web Service，root=`server`，build=`npm install && npm run build`，start=`npm start`，健康检查 `/api/health`；环境变量 `NODE_ENV=production`、`COOKIE_SAME_SITE=Lax`、`APP_ORIGIN=<Vercel 域名>`、两条数据库串、`REDEEM_CODES`（升级兑换码，逗号分隔）；迁移用 Pre-Deploy `npm run db:migrate`（或本地对 direct 串执行）。
- **Vercel**：`vercel.json` 已配置 `/api` → Render 反代与 SPA fallback（`/app` 直达应用）；确认 Render 域名后推送即可。
- 说明：同源代理下 Cookie 经 Vercel 透传，`SameSite=Lax` 即可；若改直连跨域，则需 `SameSite=None`。

### 风险
安全（HTTPS / HttpOnly+SameSite Cookie / 限流 / 密码哈希）、首次合并幂等、部署复杂度、数据隐私。

---

## 十、已知限制

- 账号与云同步已就绪（Phase 1–4）：任务/标签/「我的配色」跨端同步；当前选中的主题保持本机
- 客户端时间戳仅参与 LWW 冲突判定（推送判断已改为内容快照，不受时钟偏差影响）；时钟偏差过大时冲突取舍可能偏向快时钟设备
- 软删除的任务在"另一设备收到删除前又编辑"时按 LWW 复活（为保留"恢复"功能），永久删除与标签/配色删除为终态不可复活
- 服务端依赖 PostgreSQL；已加**内存限流**（auth 20/min、feedback 5/min、sync 120/min）与 **2MB 请求体上限**（`server/src/middleware/rate-limit.ts`、`hono/body-limit`），限流为单实例内存态、重启后清零；生产部署需配置 HTTPS 与 `APP_ORIGIN`
- 无密码找回（决定本期不做）
- 纯逻辑有单测（`npm test`），但组件/hook/同步引擎仍依赖手动验证；后端 `tsc` + PGlite 验证过同步 SQL
- 自定义主题 CSS 依赖 `color-mix`（已生成回退色，现代浏览器无碍）

---

## 十一、关键文件速查

| 需求 | 文件 |
|---|---|
| 任务逻辑 | `src/hooks/useTasks.ts` |
| 标签逻辑 | `src/hooks/useTags.ts` |
| 主题逻辑 | `src/hooks/useTheme.ts` |
| 账号状态 | `src/hooks/useAuth.ts` + `src/lib/auth.ts` |
| 账号弹窗 | `src/components/AuthPanel.tsx` |
| 同步引擎 | `src/hooks/useSync.ts` + `src/lib/sync.ts` + `src/lib/merge.ts` |
| 自定义主题同步 | `src/hooks/useTheme.ts`（`SavedPalette`）+ `server/src/sync/routes.ts` |
| 标题/副标题 | `src/hooks/useProfile.ts` + `src/components/AppHeader.tsx` |
| 意见反馈 | `src/components/FeedbackPanel.tsx` + `src/lib/feedback.ts` + `server/src/feedback/routes.ts` |
| 鉴权后端 | `server/src/auth/`（routes / middleware / password / session） |
| 同步后端 | `server/src/sync/routes.ts` |
| 升级权益 / 兑换码 | `server/src/entitlements/routes.ts` + `src/lib/entitlements.ts` + `src/hooks/useEntitlements.ts` + `src/components/UpgradePanel.tsx`（+ `UpgradePrompt.tsx`） |
| 数据表与连接 | `server/src/db/schema.ts` + `server/src/db/index.ts` |
| 存储读写 | `src/lib/storage.ts` |
| 颜色推导 | `src/lib/customTheme.ts` + `src/lib/color.ts` |
| 主题 / 主题分组 / 推荐配色 | `src/lib/themes.ts`（Mode 分组元数据） / `src/lib/palettes.ts` |
| 主界面 | `src/App.tsx` |
| 宣传落地页 / 路由 | `src/landing/`（LandingPage / AppPreview / ThemeShowcase）+ `src/Root.tsx` |
| PWA / Service Worker | `vite.config.ts`（VitePWA）+ `src/main.tsx`（registerSW）+ `public/` 图标 |
| 主题样式变量 | `src/index.css` |
| 版本日志 | `src/lib/version.ts` |
| 防闪烁脚本 | `index.html` |

---

## 十二、下一步规划：主题与买断制（A/B 已落地）

> 方向已与用户确认：**轻量主题分组（只做命名/编组，不做角色美术）** + **一次性买断制**；本期先做「架构 + 权益」，**不接支付**。开始前先读本节。

### 架构（已收敛为「分组标签 + 主题」）
| 层 | 作用 | 例子 |
|---|---|---|
| **Mode（内部元数据）** | 仅用于给主题分组，**不再是用户可见的切换 tab** | `minimal` / `anime`(二次元) / `guofeng`(国风) |
| **Theme** | 沿用现有 CSS 变量配色体系 | 清新、青黛、太阳公主… |

- 砍掉「Skin Pack 资产包」：不做立绘、头像、背景、角色设计（成本与商业性价比考量）。
- 二次元 = 用「角色气质」命名主题，**不得使用真实 IP 名/形象**（命名与配色可后续替换）。
- 国风 = 国风命名 + 传统色体系，后续持续补充。
- 二次元 / 国风主题**并入「自定义主题」面板内展示**（升级版可见），主列表只保留极简 12 套。

### 已落地（A 阶段：主题分组）
- `src/lib/themes.ts`：`ModeId`、`MODES`、`ThemeOption.mode`、`themesForMode` / `themesInMode` / `themeMode`，并预留 `locked?`。
- 新增 14 套主题：国风 `青黛`、`胭脂`；二次元 12 套（486、银发精灵、幼女、红鬼、青鬼、强欲、太阳公主、松墨、狐狸、6154、加护、神）。
- 二次元 12 套配色由 `deriveCustomTheme`（`src/lib/customTheme.ts`）按「背景 / 辅助 / 主色」推导，自动保证文字对比度；`index.css` 中为静态变量块。
- 现有主题、主布局、同步逻辑均未改动。

### 已落地（B 阶段：权益 + 自定义入口锁定，未接支付）
- 后端 `entitlements` 表（迁移 `0005_entitlements.sql`）+ `GET /api/me/entitlements` + `POST /api/me/redeem`（兑换码来自环境变量 `REDEEM_CODES`），复用 `requireAuth`，**不进 `/api/sync`**。
- 前端 `useEntitlements`（拉取 + `todo-app:entitlements` 本地缓存离线宽限 + 兑换）、`lib/entitlements.ts`。
- 仅「自定义主题」入口加锁（内含二次元 / 国风推荐主题）；新增底部「升级版」入口 + `UpgradePanel` 升级弹窗（权益说明 + 兑换码）。
- 标题 / 副标题仅升级后可改：基础版渲染为纯文本、点击不弹窗、无锁标；升级后才可点击编辑。
- 降级：权益解析完成后，若当前主题属于二次元 / 国风但未解锁，自动回退 `fresh`。
- 解锁路径为「登录 + 兑换码（手动发码）」；买断支付（Phase C）尚未接入。

### 产品分层（一次性买断）
| | 基础版（免费） | 升级版（买断） |
|---|---|---|
| 核心效率 | 任务/标签/筛选/排序/回收站/统计/PWA | 同左 |
| **云同步** | ✅ **保持免费**（口碑与增长引擎） | 同左 |
| 配色 | 极简 12 套 + 跟随系统 | 同左 |
| 自定义主题 | ❌（入口加锁） | ✅ 自选配色、自命名、保存「我的主题」 |
| 二次元 / 国风 主题 | ❌（自定义入口内） | ✅ 解锁，随自定义主题一起提供 |
| 标题 / 副标题可改 | ❌（纯文本，点击不弹窗） | ✅ |
| 阶段性报告 | 仅现有基础统计 | ✅ 周报 / 月报（后续再做） |

### 为什么同步保持免费
- 同步是**持续成本**（Render/Neon），买断是**一次性收入**，两者错配；塞进买断等于长期替非付费用户垫服务器钱。
- 同步是「本地优先、多设备」的信任基础，锁掉会削弱免费入口与转化。
- 若日后成本/留存需要，再单独引入「低价订阅（同步 + 报告）」；本阶段不做。

### 阶段性报告方向（升级版，后开发）
- 在现有统计上做「时间维度 + 对比 + 趋势」：
  - 周报：完成/新增/净变化、完成率、连续完成天数、最活跃日、标签分布、逾期清单、下周建议
  - 月报：趋势折线/柱状、环比上月、完成热力图、标签效率、活跃时段
- 形式：应用内报告卡片 → **一键生成长图分享**（传播/拉新）→ 导出 CSV/PDF。
- 触发：每周一 / 每月 1 号，PWA 通知。
- ⚠️ 永久删除会销毁历史，报告要准需「按周聚合的本地快照」；此项决定实现复杂度，**待用户确定形式偏好后再开工**。

### 权益后端（Phase B 已实现）
- 表：`entitlements`（`user_id, feature/pack_id, source, status, granted_at, expires_at`）或简化为 `users.plan`；买断记 `purchases`（`user_id, product_id, provider_payment_id, purchased_at`）。
- 接口：`GET /api/me/entitlements` → `{ plan, features: string[], expiresAt }`；买断为无过期记录。
- 关系：复用 `requireAuth`；**权益不进 `/api/sync`**，服务端权威、客户端只读并本地缓存做离线宽限。
- 一次性内购：iOS 为 non-consumable，需 IAP + 恢复购买；安卓/Web 走聚合支付/Stripe。

### 前端实现（Phase B 已落地）
- 免费用户：仅「自定义主题」入口加锁（锁标），点击打开升级弹窗；二次元 / 国风只在解锁后的自定义页内展示。
- 升级用户：自定义主题（自命名 + 分类）、二次元 / 国风主题、标题副标题、报告。
- 降级 / 离线：权益缓存带有效期，拉取失败不降级已有权益；若主题属于未解锁分组则回退 `fresh`。
- 升级入口：底部「升级版」按钮、自定义主题入口，两处都打开 `UpgradePanel`。

### 关键约束与风险
- **不做美术**：放弃立绘/头像/背景，省成本、提性价比。
- **IP**：不得使用真实动画角色名/形象，只能「颜色 + 抽象安全名」，情绪触动力有限。
- **软付费墙**：主题是打包进前端的 CSS，买断标记在客户端，防君子不防小人；可接受。
- **买断无复购**：一次性收入无法覆盖持续成本，故同步免费、报告后置；必要时再上订阅。
- **标题/副标题**：价值薄，仅作赠品，不宣传。
- **自定义分类**：若跟账号同步需改 `palettes` 表 + `/api/sync`；本阶段建议**仅存本机**（与当前选中主题一致），避免动同步。
- **合规**：自动续费（若做订阅）、隐私政策、发票；国内备案/实名等。

### 分阶段
- **A**：主题分组（内部 Mode 元数据）+ 国风 2 套 + 二次元 12 套。（**已落地**）
- **B**：`entitlements` + 兑换码解锁 + 自定义入口锁定 + 升级弹窗，跑通「锁定 → 解锁 → 降级」。（**已落地：手动发码，未接支付**）
- **C**：一次性买断支付 + 恢复购买 + 权益打通。
- **D**：自定义主题分类落库 / 同步、阶段性报告、季节限定 / 收集玩法等留存。
